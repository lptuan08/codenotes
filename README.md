# CodeNotes

CodeNotes is a PHP project.

## Branch

Current branch: `master`

## GitHub setup

Add your GitHub remote, then push this branch:

```bash
git remote add origin <your-github-repository-url>
git push -u origin master
```

Replace `<your-github-repository-url>` with your repository URL from GitHub.
